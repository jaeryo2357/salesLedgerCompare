# 브라우저 전용 배포본

`index.html`, `app.js`, `style.css`만 정적 호스팅에 올리면 됩니다.

- 서버 API·데이터베이스·`npm install`이 필요 없습니다.
- 엑셀 파일은 브라우저 메모리에서만 처리되며 외부 서버로 전송되지 않습니다.
- B 파일의 셀 위치·값·시트는 바꾸지 않고 비교 결과 셀의 색만 변경합니다.

엑셀 처리 라이브러리 코드는 jsDelivr CDN에서 내려받지만, 사용자가 선택한 파일은 CDN이나 호스팅 서버로 전달되지 않습니다.
